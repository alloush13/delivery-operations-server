# Delivery Operations Server

Backend service built with **Node.js + TypeScript + Express + Socket.IO + MongoDB**.

---

## 🚀 Requirements

- Node.js >= 18
- Docker & Docker Compose (recommended)
- MongoDB (if running without Docker)

---

## 📦 Installation

### 1. Clone project

```bash
git clone https://github.com/alloush13/delivery-operations-server.git
cd delivery-operations-server
```
### 2. Build & Run

```bash
docker compose build
docker compose up
```

# 🧪 Socket Testing (HTML Files)

For testing Socket.IO functionality, a set of **HTML test files** has been added inside the project:
